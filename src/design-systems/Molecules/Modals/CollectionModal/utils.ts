import { CollectionModalProps } from './interface'

export const mockCollectionItem: CollectionModalProps = {
  onClose: () => {},
  height: 80,
  width: 80,
  NFTDetails: [],
  changeNFT: async (data: any) => {},
}
